{
  "author": "QaldeK",
  "bugs": {
    "url": "https://github.com/encas-parka/hugo-enka-cookbook/issues"
  },
  "comments": {
    "dependencies": {
      "@hyas/bootstrap": "project",
      "@hyas/images": "project",
      "@hyas/seo": "project",
      "flatpickr": "project",
      "netlify-identity-widget": "project",
      "vuetify": "project"
    },
    "devDependencies": {
      "auto-changelog": "project",
      "decap-server": "project",
      "gethyas": "project",
      "prettier": "project",
      "prettier-plugin-go-template": "project"
    }
  },
  "dependencies": {
    "@hyas/bootstrap": "^1.0.4",
    "@hyas/images": "^2.0.3",
    "@hyas/seo": "^2.1.0",
    "flatpickr": "^4.6.13",
    "netlify-identity-widget": "^1.9.2",
    "vuetify": "^2.7.1"
  },
  "description": "Hugo theme for cookbook site",
  "devDependencies": {
    "auto-changelog": "^2.4.0",
    "decap-server": "^3.0.1",
    "gethyas": "^2.2.2",
    "prettier": "2.8.8",
    "prettier-plugin-go-template": "0.0.13"
  },
  "homepage": "https://github.com/encas-parka/hugo-enka-cookbook#readme",
  "keywords": [
    "hugo-theme",
    "SSG",
    "cookbook-site"
  ],
  "license": "GPL-3.0-or-later",
  "name": "@qaldek/hugo-enka-cookbook",
  "repository": {
    "type": "git",
    "url": "https://github.com/encas-parka/hugo-enka-cookbook.git"
  },
  "scripts": {
    "build": "exec-bin node_modules/.bin/hugo/hugo --minify ",
    "build:demo": "exec-bin node_modules/.bin/hugo/hugo --minify ",
    "clean": "npm run clean:build && npm run clean:lint && npm run clean:install",
    "clean:build": "shx rm -rf public resources .hugo_build.lock",
    "clean:install": "shx rm -rf node_modules package-lock.json yarn.lock pnpm-lock.yaml",
    "dev:demo": "exec-bin node_modules/.bin/hugo/hugo server --bind=0.0.0.0 --disableFastRender --baseURL=http://localhost --noHTTPCache --enableGitInfo --environment 'demo'",
    "dev": "exec-bin node_modules/.bin/hugo/hugo server --bind=0.0.0.0 --disableFastRender --baseURL=http://localhost --noHTTPCache",
    "dev:drafts": "exec-bin node_modules/.bin/hugo/hugo server --bind=0.0.0.0 --disableFastRender --baseURL=http://localhost --noHTTPCache --buildDrafts",
    "info": "npm list",
    "prebuild": "shx rm -rf node_modules/.bin/hugo && shx mkdir node_modules/.bin/hugo && shx cp node_modules/gethyas/node_modules/.bin/hugo/* node_modules/.bin/hugo",
    "preinfo": "npm version",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "version": "0.8.8"
}
